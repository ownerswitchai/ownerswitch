{
  "name": "@ownerswitchai/button",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "bin": { "ownerswitch-button": "dist/cli.js" },
  "dependencies": {
    "@ownerswitchai/control-plane": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
