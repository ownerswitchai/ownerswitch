/**
 * @ownerswitchai/control-plane — kill state, veto windows, 2GO restore.
 * Modules land via the pre-built feature branches; exports are wired
 * up in the integration pass once the PRs are merged.
 */
export {};
