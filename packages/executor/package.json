{
  "name": "@ownerswitchai/executor",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "bin": {
    "ownerswitch-merge-broker": "dist/merge-broker-cli.js"
  },
  "dependencies": {
    "@ownerswitchai/gateway": "workspace:*",
    "@ownerswitchai/shared": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
