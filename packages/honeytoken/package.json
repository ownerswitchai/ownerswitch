{
  "name": "@ownerswitchai/honeytoken",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "bin": { "ownerswitch-honeytoken": "dist/cli.js" },
  "dependencies": {
    "@ownerswitchai/control-plane": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
